[package]
name = "ternary-noether"
version = "0.1.0"
edition = "2021"
description = "Noether's theorem for discrete ternary systems"
license = "MIT"

[dependencies]
ternary-types = { git = "https://github.com/SuperInstance/ternary-types.git", features = ["std"] }
